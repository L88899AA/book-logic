# 贡献指南

## 开发环境

要求 Python 3.11+ 和支持 FTS5 的 SQLite。在源码目录创建并激活虚拟环境后执行：

```sh
python -m pip install -e '.[dev]'
python -m unittest discover -s tests -p 'test_runtime*.py' -v
```

可选索引测试依赖 NumPy。完整验证流程见 [测试与评估](docs/validation.md)。

## 修改原则

- 保持来源坐标、不可变成果版本和依赖失效机制的一致性。
- 变更 schema、策略或提示契约时，同步更新运行时、Skill、文档及行为测试。
- 修改持久化格式时，明确版本兼容或迁移方案。
- 不将引文匹配、任务状态或处理覆盖率描述为语义正确性的证明。
- 测试使用合成数据或已获分发许可的最小样本，不依赖个人目录和书库。

## 提交变更

提交说明应包含变更目的、影响范围、兼容性及验证命令。缺陷报告应提供可复现步骤、预期行为、实际行为、运行环境和脱敏后的最小输入。

不提交书籍全文、用户资料库、模型权重、凭证或包含敏感信息的执行日志。安全问题按 [安全说明](SECURITY.md) 处理。

## 评估要求

真实模型评估需记录模型标识、语料范围、样本数量、评审标准，以及查询或提示是否经过样本调优。测试编码器仅用于协议验证。模型质量结论应有对应评估证据，不能以固定案例回放代替。

## 发布检查

1. 运行测试、协议回放与检索回归。
2. 构建并检查 wheel/sdist，在独立环境验证安装。
3. 检查 Skill、schema、策略资源与依赖声明是否完整。
4. 审查 Git 暂存区、历史和发布附件中的敏感数据与许可证。

仓库职责划分见 [仓库结构](DIRECTORY.md)。
